#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char** argv)
{

    // add algorithm

    // read in plaintext 
    // L read in 16 bytes at a time

    // read in s-box

    // generate round keys given key
    // L 14 rounds for a 256-bit key

    // perform round operations:
    // L sub-bytes
    // L shift rows
    // L mix columns
    // L add round key

    printf("Hello World!\n");

    return 0;
}